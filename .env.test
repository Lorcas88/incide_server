NODE_ENV=test

# Database (Test)
DB_HOST=localhost
DB_USER=incide_user_test
DB_PASSWORD=password
DB_NAME=incide_db_test
DB_PORT=3307

# Security (Test)
JWT_SECRET=test_secret_for_testing_only_minimum_32_characters
JWT_EXPIRES_IN=1h
BCRYPT_ROUNDS=10

# Email Service (Test - uses test key)
RESEND_API_KEY=re_test_key_for_testing_only

# Client/Frontend (Test)
FRONTEND_URL=http://localhost:5173